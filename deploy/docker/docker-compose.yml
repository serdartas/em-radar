services:
  emradar:
    build:
      context: ../..
      dockerfile: deploy/docker/Dockerfile
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - emradar-data:/data

volumes:
  emradar-data:
